# MCP Server configuration

# Slack settings
SLACK_BOT_TOKEN=your-slack-bot-token

# Gmail (optional) oauth credentials
# Provide a path to your credentials.json, or leave empty if not using Gmail
GMAIL_CREDENTIALS_JSON=credentials.json

# Jira — replace with your actual Jira details if you integrate Jira logic later
JIRA_EMAIL=
JIRA_API_TOKEN=
JIRA_BASE_URL=https://your-domain.atlassian.net

